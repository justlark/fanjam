/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected by Vite at build time. `undefined` when the build had no git metadata to read.
declare const __BUILD_VERSION: string | undefined;
